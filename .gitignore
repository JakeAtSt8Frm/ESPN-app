# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.cache
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Raw multi-season ESPN history. Regenerated by `npm run snapshot`, read only
# by the Node-side fits, and eight megabytes — the browser reads the distilled
# `public/data/priors.json` instead.
history

# Local credentials — never commit
.env
.env.local
